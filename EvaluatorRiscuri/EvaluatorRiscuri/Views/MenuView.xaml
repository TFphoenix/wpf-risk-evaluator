<UserControl x:Class="EvaluatorRiscuri.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EvaluatorRiscuri.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--Header-->
        <WrapPanel Grid.Row="0" VerticalAlignment="Center">
            <Button Style="{StaticResource MaterialDesignRaisedAccentButton}" Click="Logout_OnClick">
                <WrapPanel>
                    <Image Source="../Resources/logout.png" Width="20" Margin="0 0 5 0"/>
                    <Label Foreground="{DynamicResource MaterialDesignDarkForeground}">Deconectare</Label>
                </WrapPanel>
            </Button>
        </WrapPanel>

        <!--Content-->
        <StackPanel Grid.Row="1">
            <Button>Test</Button>
        </StackPanel>
    </Grid>
</UserControl>
